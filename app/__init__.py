# app/ — Streamlit UI package (Phase 6 PUI-X)
#
# PSA-2 (Phase 5A) creates app/main.py as a placeholder so the Cloud
# Run container has something to serve while the real UI is being
# built. Phase 6 PUI-1 expands main.py into the multi-page Streamlit
# shell (Dashboard / Inventory / Codify / Drift / Policy / Settings).
